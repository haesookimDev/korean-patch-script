apiVersion: v2
name: company-ai-gateway
description: Example wrapper chart for the GitLab AI Gateway
type: application
version: 0.1.0

# templates/ 없이 dependency chart의 리소스만 렌더링한다.
dependencies:
  - name: ai-gateway
    version: 0.15.1
    # 폐쇄망 내부 Helm repository 주소로 교체한다.
    repository: https://helm.example.internal/repository/gitlab-ai-gateway
